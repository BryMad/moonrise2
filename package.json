{
  "name": "moonrise-tracker",
  "version": "1.0.0",
  "description": "Professional astronomical moonrise tracking application",
  "main": "backend/server.js",
  "scripts": {
    "server-build": "cd backend && npm i",
    "frontend-build": "cd frontend && npm run build-install",
    "build": "npm run server-build && npm run frontend-build",
    "start": "cd backend && npm start"
  },
  "keywords": [
    "astronomy",
    "moonrise",
    "moon",
    "tracker",
    "calendar",
    "photography"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
